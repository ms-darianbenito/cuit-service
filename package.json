{
  "name": "cuit-service",
  "private": true,
  "dependencies": {
    "eclint": "^2.8.1",
    "prettier": "^2.0.4"
  },
  "scripts": {
    "verify": "yarn prettier-check && yarn eclint-check",
    "eclint-fix": "eclint fix $(git ls-files)",
    "eclint-check": "eclint check $(git ls-files)",
    "prettier-fix": "prettier --write \"**/**\"",
    "prettier-check": "prettier --check \"**/**\""
  }
}
